You are the fresh independent checker for a non-formal cold-debug rehearsal. Read `brief.txt`, `/workspace/subject/artifacts`, `/workspace/subject/commands.json`, `/workspace/input/debug-evidence`, and `/workspace/input/hidden-mutation.json`. Work only in `/workspace/output`. Check the diagnosis against the hidden mutation, reproduce any proposed content repair with `/workspace/bin/nomos`, and write `/workspace/output/checker.json` with `verdict` equal to `pass` or `reject`, exact commands and hashes, and concise rubric reasons. Do not access the network or paths outside `/workspace`. This check is not a formal Gate K attempt.
