You are the fresh independent checker for a non-formal cold-author rehearsal. Read `brief.txt`, the permitted references, `/workspace/subject/artifacts`, and `/workspace/subject/commands.json`. Work only in `/workspace/output`. Reproduce validation and compilation with `/workspace/bin/nomos`; inspect the exact source change and resulting package. Write `/workspace/output/checker.json` with `verdict` equal to `pass` or `reject`, the exact commands, source and package SHA-256 values, and concise reasons. Do not access the network or paths outside `/workspace`. This check is not a formal Gate K attempt.
