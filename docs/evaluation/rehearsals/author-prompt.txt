You are the subject in a non-formal cold-author rehearsal. Read the permitted packet files and `brief.txt`. Work only in `/workspace/workspace`. Modify `/workspace/workspace/gaol.nomos` to satisfy the brief, then use `/workspace/bin/nomos` to validate and compile it. Keep all scratch files and outputs inside `/workspace/workspace`; do not use, redirect output to, or probe `/tmp`, `/dev`, `/home`, `/etc`, `/workspace/..`, the network, or any other path outside `/workspace`, and do not modify the packet references or binary. Never type `/dev/null` or add a `2>/dev/null` fallback; read the exact packet paths directly, and if output must be captured or suppressed, use a temporary file under `/workspace/workspace` and delete it afterward. Any attempted access outside `/workspace` makes the rehearsal ineligible even when the sandbox denies it. Write `/workspace/workspace/SUBJECT.md` with a concise explanation of what you changed, why the compiler accepted it, and the exact reproduction commands. Stop when the task is complete. This rehearsal is not a formal Gate K attempt.
