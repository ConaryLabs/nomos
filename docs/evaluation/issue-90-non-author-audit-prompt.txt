You are the independent non-author auditor for Nomos issue #90. Review the
exact detached Git commit currently checked out. You did not author this
implementation. Do not modify, create, or delete any tracked file.

First read AGENTS.md, README.md, THESIS.md, KERNEL.md,
docs/decisions/0013-gate-k-disposition.md,
docs/decisions/0015-gate-k-round-two.md, docs/HANDOFF.md, and issue #90 from
the supplied brief. Inspect the complete diff from origin/main through HEAD and
all files it changes.

Audit issue #90 against these requirements:

- the candidate-lineage proof resolves the exact annotated gate-k-rc1 commit,
  requires it to be an ancestor, and binds the proposed full candidate commit
  and tree;
- the kernel crates, fixtures, Cargo/toolchain inputs, xtask, workflows,
  THESIS.md, and every active root-level public semantic document under docs/
  are byte-identical to gate-k-rc1;
- KERNEL.md admits only the exact decision-0013 status, disposition-record, and
  disposition-paragraph edit while preserving contract revision 7 and every
  criterion byte-for-byte;
- every path changed since gate-k-rc1 is exhaustively and deterministically
  classified as evaluation tooling/evidence, owner decision, review evidence,
  quarantined static experiment, status/hygiene, or the exact KERNEL
  disposition; an unknown path fails closed;
- decision 0013, the four frozen round-one task receipts, both failed results,
  and the four-event formal-attempt ledger are exact hash matches, so no formal
  reservation or launch has been added;
- adversarial tests reject a protected input change, a non-disposition KERNEL
  edit, a frozen-record mutation, and an unclassified path;
- README.md and docs/HANDOFF.md accurately close issue #88 / PR #89, identify
  issue #90 as the candidate-freeze slice, and forbid formal work until the
  exact merged candidate passes both workflows and receives its annotated tag;
- no provider launch, candidate tag, semantic change, protocol/rubric change,
  renderer work, or Gate K pass claim occurs in the reviewed commit.

Run and independently assess all of these commands:

git status --short
git diff --check origin/main...HEAD
docs/evaluation/test-gate-k-candidate-lineage.sh
python3 docs/evaluation/gate-k-candidate-lineage.py gate-k-rc1 HEAD
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked
cargo xtask boundary
docs/evaluation/test-pi-cold-agent-preflight.sh
docs/evaluation/test-gate-k-eval-tooling.sh

Also verify that the checkout remains clean after review. Treat any substantive
correctness, evidence-integrity, acceptance, regression, security, or
maintainability defect as a finding. Do not invent stylistic findings. A pass
requires zero findings and every required command passing.

Your final response must be exactly one JSON object and no Markdown. Use this
shape:

{
  "schema": "nomos.issue90.non_author_audit@1",
  "reviewedCommit": "full 40-character HEAD commit",
  "reviewer": {
    "provider": "deepseek",
    "model": "exact model ID",
    "freshSession": true,
    "nonAuthor": true
  },
  "status": "pass or fail",
  "findings": [
    {
      "severity": "blocking, major, or minor",
      "location": "file and line or evidence path",
      "summary": "concise finding",
      "evidence": "specific supporting evidence"
    }
  ],
  "verification": [
    {
      "command": "exact command",
      "status": "pass or fail",
      "details": "concise observed result"
    }
  ],
  "protectedSurfaceUnchanged": true,
  "roundOneEvidencePreserved": true,
  "formalLedgerUnchanged": true,
  "checkoutCleanBeforeAndAfter": true,
  "notes": ["concise evidence-backed note"]
}
