You are the independent non-author evidence auditor for Nomos issue #94. Review
the exact detached Git commit currently checked out. You did not author this
implementation. Do not modify, create, or delete any tracked file. Do not act
as the formal Gemini checker and do not independently adjudicate the hidden
mutation; issue #96 owns that fresh semantic checker operation.

First read AGENTS.md, README.md, THESIS.md, KERNEL.md,
docs/decisions/0015-gate-k-round-two.md,
docs/evaluation/COLD_AGENT_PROTOCOL.md,
docs/evaluation/GATE_K_COLD_AGENT_PLAN.md, and issue #94 as quoted below.
Inspect the complete diff from origin/main through HEAD and every changed file.

Issue #94 requires one fresh eligible DeepSeek-family cold-debug subject against
annotated candidate `gate-k-rc2`, with the exact route, seeded packet, prompt,
hashes, writable boundary, rubric, and revision-6 plan pinned before launch; a
committed hash-chained reservation; a later distinct committed launch marker
only after a passing neutral preflight; one launch with zero coaching and zero
retry; a complete authenticated subject record; a mechanically derived
subject-stage outcome; a closed ledger; a non-author audit; and no Gemini
checker or Gate K verdict.

Audit the exact-head record against these requirements:

- `gate-k-rc2` is the unique annotated tag for
  `53db236d397b3db0779f0d2aab23180d926e55a5`, and the committed declaration
  records both successful exact-commit workflow URLs;
- the fixed brief, revision-6 prompt, formal plan, packet manifest, seeded
  failing run, sealed mutation, candidate binary, provider/model/thinking route,
  Pi identity, writable paths, rubric, and sole `/dev/null` exception are
  mutually consistent and hash-bound before launch;
- ledger events 8-10 are canonical and hash-chained reservation, launch, and
  authenticated close events for `gate-k-rc2-debug-subject`, and Git history
  proves the reservation and launch were distinct prior commits;
- the disclosed blocked seed-generation commands and neutral probe occurred
  before packet construction or formal launch as recorded, and did not consume
  or alter the formal attempt;
- the later neutral probe passed on a clean standalone checkout at the exact
  candidate, with the exact eligible DeepSeek identity and boundary;
- exactly one formal subject session launched after the marker, with no operator
  coaching or retry, and the task receipt binds the launch-marker commit,
  packet, identity, boundary, complete event stream, commands, accounting, and
  artifacts;
- every recorded subject command stays within `/workspace`; exact `/dev/null`
  uses are allowed only as non-information-bearing sinks, and no other outside
  path, network, hidden context, repository mount, or undeclared top-level tool
  is present;
- immutable packet members remain unchanged, all writes stay under
  `/workspace/output`, the failing run is reproduced, and the proposed content
  repair is actually verified;
- STATUS.md truthfully distinguishes the correct causal diagnosis from the
  subject's differing repair choice, keeps semantic merit pending issue #96,
  and claims neither a cold-debug pass nor a Gate K verdict;
- the task recorder mechanically derives `eligible-for-checker`, the ledger is
  closed, and no kernel, fixture, protocol, rubric, renderer, Gate 1, checker,
  or unrelated project change is present.

Run and independently assess all of these commands:

git status --short
git diff --check origin/main...HEAD
git tag -n99 gate-k-rc2
git rev-parse gate-k-rc2^{}
git log --format='%H %s' --reverse origin/main..HEAD
python3 docs/evaluation/gate-k-eval-attempt-ledger.py validate docs/evaluation/gate-k-formal-attempt-ledger.jsonl
docs/evaluation/gate-k-eval-finalize.sh --validate-task-record docs/evaluation/runs/gate-k/2026-08-24-deepseek-v4-flash-vision-exp-debug-rc2/subject-task
docs/evaluation/test-gate-k-attempt-ledger.sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked
cargo xtask boundary
docs/evaluation/test-pi-cold-agent-preflight.sh
docs/evaluation/test-gate-k-eval-tooling.sh

Also verify the checkout remains clean after review. Treat any substantive
correctness, evidence-integrity, acceptance, regression, security, or
maintainability defect as a finding. Do not invent stylistic findings. A pass
requires zero findings and every required command passing. A semantic mismatch
already disclosed and explicitly deferred to the formal checker is not itself
an evidence-audit failure unless the record hides, misstates, or pre-adjudicates
it.

Your final response must be exactly one JSON object and no Markdown. Use this
shape:

{
  "schema": "nomos.issue94.non_author_audit@1",
  "reviewedCommit": "full 40-character HEAD commit",
  "reviewer": {
    "provider": "anthropic",
    "model": "exact model ID",
    "freshSession": true,
    "nonAuthor": true,
    "formalChecker": false
  },
  "status": "pass or fail",
  "findings": [
    {
      "severity": "blocking, major, or minor",
      "location": "file and line or evidence path",
      "summary": "concise finding",
      "evidence": "specific supporting evidence"
    }
  ],
  "dimensions": {
    "semanticMerit": "diagnosis-recorded-repair-mismatch-pending-independent-checker or fail",
    "independenceIntegrity": "pass or fail",
    "operationalCompliance": "pass or fail"
  },
  "subjectOutcome": "eligible-for-checker or fail",
  "verification": [
    {
      "command": "exact command",
      "status": "pass or fail",
      "details": "concise observed result"
    }
  ],
  "formalLedgerClosed": true,
  "checkerNotLaunched": true,
  "protectedSurfaceUnchanged": true,
  "checkoutCleanBeforeAndAfter": true,
  "notes": ["concise evidence-backed note"]
}

Before sending the response, pass its exact bytes through a strict JSON parser
such as `python3 -c 'import json,sys; json.load(sys.stdin)'` or `jq -e .`. Do not
send it until that check succeeds. Byte 1 of the response must be `{` and the
final non-whitespace byte must be `}`. A Markdown code fence, language label,
preface, validation message, or trailing commentary makes the audit record fail
even when the object inside it is valid. Emit the parsed object directly as a
single-line JSON value.
