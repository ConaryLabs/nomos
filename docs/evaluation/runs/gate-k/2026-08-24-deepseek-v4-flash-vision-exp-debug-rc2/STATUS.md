# Gate K round-two cold-debug subject status

The one formal DeepSeek cold-debug subject transport is complete and the
hash-chained attempt is closed. The authenticated task receipt derives the
subject-stage outcome `eligible-for-checker`; it does not claim a cold-debug
pass or a Gate K verdict.

## Subject-stage dimension disposition

- Semantic merit: the preserved subject artifacts correctly identify that
  `ignite north_gate` commits combustion and causal integrity transitions while
  leaving the independent access machine `locked`; `open north_gate` is then
  illegal. The subject cited the state, receipt, projection-delta, package, and
  diagnostic evidence and excluded more than two alternatives. Its verified
  repair adds a credentialed unlock before retaining `open`, however, while the
  sealed mutation expects the inserted `open` to be removed so the repaired
  script preserves the intended three-command integrity-derived traversal
  demonstration. Formal semantic adjudication remains pending the fresh
  independent Gemini checker required by issue #96.
- Independence integrity: the authenticated boundary and receipt record the
  exact eligible DeepSeek route, one fresh ephemeral formal session, zero
  operator intervention, zero retries, no mounted repository, and only the
  declared packet information.
- Operational compliance: the complete 63-command record stays under
  `/workspace`, writes only under `/workspace/output`, preserves every immutable
  packet member, and is bound to the receipt. Recorded uses of the exact
  `/dev/null` device are non-information-bearing redirections allowed by
  protocol revision 6. No other outside path or network request is present.

The transport completed with status `0`; task receipt SHA-256 is
`8e57b5017846431a5eb0255276e3fe3d3501ea8579d6383567e7b55f231ee2b8`.
The formal ledger contains distinct committed reservation, launch, and close
events and has no open attempt.

One neutral prelaunch probe blocked before subject launch because it was invoked
from the tooling checkout rather than the standalone candidate. A later probe
from the clean standalone clone passed. Two operator-only seed-generation
commands also failed closed on absolute Nomos paths before packet construction.
All three failures are preserved under `prelaunch/`; none launched or modified
the formal subject task.

No Gemini checker ran in this issue.
