# Gate K round-two cold-author subject status

The one formal Gemini cold-author subject transport is complete and the
hash-chained attempt is closed. The authenticated task receipt derives the
subject-stage outcome `eligible-for-checker`; it does not claim a cold-author
pass or a Gate K verdict.

## Subject-stage dimension disposition

- Semantic merit: the preserved subject artifacts contain the requested second
  approved door, distinct ID `north_gate_02`, exact supplied face anchor, valid
  result explanation, successful validation output, and a complete compiled
  world. Formal semantic adjudication remains pending the fresh independent
  DeepSeek checker required by issue #95.
- Independence integrity: the authenticated boundary and receipt record the
  exact eligible Gemini route, one fresh ephemeral session, zero operator
  intervention, zero retries, no mounted repository, and only the declared
  packet information.
- Operational compliance: the complete 43-command record stays under
  `/workspace`, requests no outside path, preserves every immutable packet
  member, and is bound to the receipt. The exact `/dev/null` exception was not
  used.

The transport completed with status `0`; task receipt SHA-256 is
`98f68e53e4b0ceddaee54d049e0c0050bd39d43ed4e86043952cf6af50b876c5`.
The formal ledger contains distinct committed reservation, launch, and close
events and has no open attempt.

Two neutral prelaunch probes blocked before subject launch. The first detected
a caller working-directory mismatch; the second detected linked-worktree Git
metadata outside the sandbox. Both failures are preserved under `prelaunch/`.
A clean standalone clone of the same tagged candidate then passed the mandatory
neutral boundary probe. No prompt, packet, model, rubric, reservation identity,
or candidate bytes changed, and neither blocked probe launched the formal task.

Post-close verification also exposed a stale harness assumption: the offline
ledger and finalizer tests required the live ledger to remain byte-identical to
its four-event round-one prefix. The repair now validates the complete closed
append-only ledger and separately proves that its first four events remain the
exact frozen inventory. Round-one and revision-6 rehearsal re-finalizations
remain byte-identical. This tooling repair does not alter the closed task record.

No DeepSeek checker ran in this issue.
