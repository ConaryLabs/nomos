# Author notes

- `gaol verify` runs: 1
- Diagnostics encountered:
  - `EK0804` reported that each baseline scenario's gate could not be opened
    while locked. This is the expected rejected baseline copied from the
    required scenario sequence, so it needed no fix; `gaol verify` accepted it
    as scenario evidence.
  - No authoring or verification failure diagnostics were encountered.
- Packet gaps: the permitted presentations expose the other areas' masonry
  masses, but not their gate columns, water regions, or brazier cells. Those
  three aspects therefore could not be manually compared against every area
  without crossing the cold-author read boundary. No undocumented vocabulary
  or implementation detail was needed to finish, and the shared-grammar and
  collection verification passed.
