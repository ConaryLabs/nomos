[package]
name = "nomos-schema"
description = "Authoring, stable World IR, construction, and package-registry schemas for Gate K."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

# KERNEL.md section 10 permits exactly one edge: nomos-schema -> nomos-core.
[dependencies]
nomos-core.workspace = true
