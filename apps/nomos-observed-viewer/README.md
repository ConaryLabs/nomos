# Nomos observed-scene viewer

This isolated offline evidence consumer reads only compiled
`nomos.observed_scene_plan@1` artifacts, its fixed renderer catalog, and the
generated integrity index. It owns no gameplay fact and imports no R1 viewer
source. Build and proof commands are recorded by issue #197 and `R2.md`.
